<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:flex="http://www.springframework.org/schema/flex"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="
    	http://www.springframework.org/schema/beans
        http://www.springframework.org/schema/beans/spring-beans-3.0.xsd
        http://www.springframework.org/schema/flex
        http://www.springframework.org/schema/flex/spring-flex-1.0.xsd">

    <description>
    	Configures the Flex based requests that will be handled by this
		web application. This configuration file is picked up by spring by
     	naming convention based on the flex servlet declaration in the web.xml
    </description>

    <flex:message-broker>
		<flex:message-service default-channels="my-amf" />
		<flex:secured />
	</flex:message-broker>

	<!-- Helper for getting the currently authenticated user -->
    <bean id="securityHelper" class="com.poi.util.Security3Helper">
        <flex:remoting-destination/>
    </bean>

</beans>